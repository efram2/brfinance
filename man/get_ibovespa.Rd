% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_ibovespa.R
\name{get_ibovespa}
\alias{get_ibovespa}
\title{Get Ibovespa Index (B3)}
\usage{
get_ibovespa(
  start_date = NULL,
  end_date = NULL,
  language = "eng",
  labels = TRUE
)
}
\arguments{
\item{start_date}{Start date for the data period. Accepts multiple formats:
\itemize{
\item \code{"YYYY"} for year only (e.g., \code{"2020"} becomes \code{"2020-01-01"})
\item \code{"YYYY-MM"} for year and month (e.g., \code{"2020-06"} becomes \code{"2020-06-01"})
\item \code{"YYYY-MM-DD"} for a specific date (e.g., \code{"2020-06-15"})
\item \code{NULL} defaults to the last 30 days, using today as the end date
}}

\item{end_date}{End date for the data period. Accepts the same formats as \code{start_date}:
\itemize{
\item \code{"YYYY"} (e.g., \code{"2023"} becomes \code{"2023-12-31"})
\item \code{"YYYY-MM"} (e.g., \code{"2023-12"} becomes the last day of December 2023)
\item \code{"YYYY-MM-DD"} for a specific date
\item \code{NULL} defaults to the current date (today)
}}

\item{language}{Language for the \code{labelled} variable descriptions attached
to the returned data.frame ("eng" or "pt"). Column names are always
\code{date} and \code{value}.}

\item{labels}{Logical indicating whether to add variable labels using the
\code{labelled} package.}
}
\value{
A data.frame with columns:
\describe{
\item{date}{Trading date}
\item{value}{Ibovespa closing level (index points)}
}
}
\description{
Downloads the daily closing level of the Ibovespa (Índice Bovespa), Brazil's
main stock market benchmark, via Yahoo Finance (ticker \verb{^BVSP}). Uses the
\code{yfR} package, the same data source already used in the \code{{brstocks}}
package, so \code{{brfinance}} now covers the equity side alongside its
macroeconomic series.
}
\note{
Requires the \code{yfR} package (\code{install.packages("yfR")}) and an internet
connection to Yahoo Finance. Unlike \verb{get_*()} functions backed by BCB/SGS,
this one is not subject to the 10-year window limit — \code{yfR} handles the
full request in one call.
}
\examples{
\donttest{
  # Default: last 12 months
  df <- get_ibovespa()

  # Specific period
  df2 <- get_ibovespa("2020-01-01", "2023-12-31")

  # Portuguese labels
  df3 <- get_ibovespa(language = "pt")
  }

}
